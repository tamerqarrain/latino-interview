ANTHROPIC_API_KEY=your_anthropic_key_here
ELEVENLABS_API_KEY=your_elevenlabs_key_here
DEEPGRAM_API_KEY=your_deepgram_key_here

# Optional: customize the ElevenLabs voice ID
# Default is "Adam" - a clear, professional male voice
# Find voice IDs at: https://elevenlabs.io/voice-library
ELEVENLABS_VOICE_ID=pNInz6obpgDQGcFmaJgB

# ── Email reports to HR ──
# Where interview reports are sent (comma-separated for multiple recipients)
HR_EMAIL=hr@yourschool.com

# Option 1 (preferred): Gmail SMTP — Google-to-Google delivery, not subject to
# any shared sender's reputation. If both vars below are set, this is used
# and Resend is ignored entirely.
#   1. Enable 2-Step Verification on the sending Google account.
#   2. Generate an App Password at https://myaccount.google.com/apppasswords
#      (name it e.g. "Latino Interview").
#   3. Set GMAIL_USER to that Google account's address, GMAIL_APP_PASSWORD to
#      the 16-character app password (no spaces).
GMAIL_USER=you@yourdomain.com
GMAIL_APP_PASSWORD=your_16_char_app_password

# Option 2 (fallback if Gmail vars above are not set): Resend.
# Get a free API key at https://resend.com (3,000 emails/month free).
# NOTE: onboarding@resend.dev is a SHARED sandbox sender — Google can silently
# drop mail from it at any time since its reputation isn't yours to control.
# For production, verify your own domain in Resend and set FROM_EMAIL to an
# address on that domain instead.
RESEND_API_KEY=your_resend_key_here
FROM_EMAIL=onboarding@resend.dev

# ── Google Sheets logging (optional, for searchable history) ──
# 1. Create a service account in Google Cloud Console
# 2. Enable Google Sheets API
# 3. Share your sheet with the service account email (as Editor)
# 4. Paste values below (the private key MUST keep \n as literal characters when stored)
GOOGLE_SHEET_ID=your_sheet_id_from_url
GOOGLE_SERVICE_ACCOUNT_EMAIL=name@project.iam.gserviceaccount.com
GOOGLE_SERVICE_ACCOUNT_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
GOOGLE_SHEET_TAB=Sheet1
